branch="$(git branch --show-current)"

if [ "$branch" = "main" ]; then
  echo "[husky] post-commit: na branch main, disparando deploy..."
  pnpm deploy:local
else
  echo "[husky] post-commit: branch \"$branch\" != main, deploy ignorado"
fi